# Backend Docs

Purpose:
- Document how concrete backends satisfy the platform contract.

Owns:
- Backend-specific notes, conformance expectations, and integration guidance.

Must not own:
- Core contract definitions or case-platform semantics.

Relation:
- Each backend implementation under `packages/platform-backends/` should eventually map to docs here.

