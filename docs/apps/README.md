# App Docs

Purpose:
- Describe the platform-facing API app and the CAI-facing orchestration app.

Owns:
- App boundary documentation, integration notes, and operator-facing behavior summaries.

Must not own:
- Core domain rules that belong in contracts or core packages.

Relation:
- Tracks the apps under `apps/`.

