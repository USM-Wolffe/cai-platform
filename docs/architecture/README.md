# Architecture Docs

Purpose:
- Describe platform boundaries, dependency rules, and repository topology.

Owns:
- Human-readable explanations of core vs adapters vs backends vs apps.

Must not own:
- Implementation-specific behavior that only exists in code.

Relation:
- Works alongside `docs/contracts/` and the package READMEs to keep architecture explicit.

