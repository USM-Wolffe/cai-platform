[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cai-platform-contracts"
version = "0.1.0"
description = "Canonical shared contract models for cai-platform-v2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2,<3",
]

[project.optional-dependencies]
test = [
  "pytest>=8,<9",
]

[tool.setuptools.packages.find]
where = ["src"]
